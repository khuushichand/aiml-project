import { useDictionaryEntryTools } from "./useDictionaryEntryTools"
import { useDictionaryEntryManagerActions } from "./useDictionaryEntryManagerActions"
import { useDictionaryEntryManagerContext } from "./useDictionaryEntryManagerContext"

type UseDictionaryEntryManagerPublicStateParams = {
  context: ReturnType<typeof useDictionaryEntryManagerContext>
  tools: ReturnType<typeof useDictionaryEntryTools>
  actions: ReturnType<typeof useDictionaryEntryManagerActions>
}

export function useDictionaryEntryManagerPublicState({
  context,
  tools,
  actions,
}: UseDictionaryEntryManagerPublicStateParams) {
  return {
    t: context.t,
    isMobileViewport: context.isMobileViewport,
    editEntryForm: context.editEntryForm,
    toolsPanelKeys: context.toolsPanelKeys,
    setToolsPanelKeys: context.setToolsPanelKeys,
    entrySearch: context.entrySearch,
    setEntrySearch: context.setEntrySearch,
    entryGroupFilter: context.entryGroupFilter,
    setEntryGroupFilter: context.setEntryGroupFilter,
    entriesStatus: context.entriesStatus,
    entriesError: context.entriesError,
    refetchEntries: context.refetchEntries,
    entries: context.entries,
    entryGroupOptions: context.entryGroupOptions,
    filteredEntries: context.filteredEntries,
    hasAnyEntries: context.hasAnyEntries,
    normalizedEntryGroupFilter: context.normalizedEntryGroupFilter,
    filteredEntryIds: context.filteredEntryIds,
    highlightedValidationEntryId: context.highlightedValidationEntryId,
    jumpToValidationEntry: context.jumpToValidationEntry,
    validationStrict: tools.validationStrict,
    setValidationStrict: tools.setValidationStrict,
    validationReport: tools.validationReport,
    validationError: tools.validationError,
    runValidation: tools.runValidation,
    validating: tools.validating,
    previewTokenBudget: tools.previewTokenBudget,
    setPreviewTokenBudget: tools.setPreviewTokenBudget,
    previewMaxIterations: tools.previewMaxIterations,
    setPreviewMaxIterations: tools.setPreviewMaxIterations,
    previewResult: tools.previewResult,
    previewError: tools.previewError,
    handlePreview: tools.handlePreview,
    previewing: tools.previewing,
    previewEntriesUsed: tools.previewEntriesUsed,
    previewProcessedText: tools.previewProcessedText,
    previewDiffSegments: tools.previewDiffSegments,
    previewHasDiffChanges: tools.previewHasDiffChanges,
    previewText: context.previewText,
    setPreviewText: context.setPreviewText,
    previewCaseName: context.previewCaseName,
    handlePreviewCaseNameChange: context.handlePreviewCaseNameChange,
    previewCaseError: context.previewCaseError,
    savedPreviewCases: context.savedPreviewCases,
    savePreviewCase: context.savePreviewCase,
    loadPreviewCase: context.loadPreviewCase,
    deletePreviewCase: context.deletePreviewCase,
    selectedEntryRowKeys: actions.selectedEntryRowKeys,
    setSelectedEntryRowKeys: actions.setSelectedEntryRowKeys,
    selectedEntryIds: actions.selectedEntryIds,
    canReorderEntries: context.canReorderEntries,
    canEscalateSelectAllFilteredEntries: actions.canEscalateSelectAllFilteredEntries,
    handleSelectAllFilteredEntries: actions.handleSelectAllFilteredEntries,
    bulkEntryAction: actions.bulkEntryAction,
    handleBulkEntryAction: actions.handleBulkEntryAction,
    bulkGroupName: actions.bulkGroupName,
    setBulkGroupName: actions.setBulkGroupName,
    entryTableColumns: actions.entryTableColumns,
    editingEntry: actions.editingEntry,
    closeEditEntryPanel: actions.closeEditEntryPanel,
    handleEditEntrySubmit: actions.handleEditEntrySubmit,
    updatingEntry: actions.updatingEntry,
    adding: actions.adding,
    advancedMode: actions.advancedMode,
    toggleAdvancedMode: actions.toggleAdvancedMode,
    handleAddEntrySubmit: actions.handleAddEntrySubmit,
    handleAddEntryPatternChange: actions.handleAddEntryPatternChange,
    handleAddEntryReplacementChange: actions.handleAddEntryReplacementChange,
    handleAddEntryTypeChange: actions.handleAddEntryTypeChange,
    regexError: actions.regexError,
    regexServerError: actions.regexServerError,
  }
}
