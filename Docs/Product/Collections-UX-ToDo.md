# Collections UX To-Do

## Reading Digest Suggestions
- Add a suggestions toggle in the digest schedule editor.
- Show fields when enabled: limit, status list, exclude tags, max age (days), include read, include archived.
- Add helper copy: suggestions are local-only and heuristic-based.
- Ensure the payload is stored under `filters.suggestions` and preserved on edit.
- Validate limits and status values client-side to match API constraints.
