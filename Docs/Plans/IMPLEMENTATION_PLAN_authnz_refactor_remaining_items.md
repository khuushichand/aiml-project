## Context
This active plan tracks unfinished work split from:
`Docs/Product/Completed/AuthNZ-Refactor/IMPLEMENTATION_PLAN.md`

## Stage 1: Profiles & Modes
**Goal**: Introduce a small set of blessed RAG profiles (e.g., production, research, cheap/fast) with documented defaults that map onto `unified_rag_pipeline` parameters without breaking existing callers.
**Success Criteria**:
- A lightweight profile abstraction exists (e.g., `RAGProfile`) with three named profiles and clear defaults.
- Callers can opt into a profile via a simple API (e.g., helper or `profile` argument) while still being able to override individual knobs.
- Documentation describes each profile's intent, defaults, and tradeoffs (latency, cost, safety).
- Unit tests cover profile-to-pipeline-args mapping and guard against regression.
**Tests**:
- New unit tests for the profile helper module.
- Adjusted/added tests in `tests/RAG_NEW` that exercise at least one profile end-to-end.
**Status**: Complete

## Stage 2: Multi-Tenant Isolation Story
**Goal**: Clarify and tighten multi-tenant isolation across DBs, caches, exemplars, and telemetry for the RAG pipeline.
**Success Criteria**:
- Design doc (or section in existing RAG docs) explicitly states tenant/user boundaries and trust assumptions.
- Caches and persisted artifacts (semantic/adaptive cache, rewrite cache, exemplars) are keyed or scoped by user/namespace/tenant where appropriate, or explicitly disabled in multi-tenant profiles.
- Telemetry/metrics and tracing include tenant-safe labels and guidance on off-box export.
- Tests and/or linters guard against obvious cross-tenant leakage patterns in new code.
**Tests**:
- Unit tests for cache keying/namespacing behavior.
- Focused tests for exemplar logging to ensure user/namespace metadata is present and redaction is applied.
**Status**: Complete

## Stage 3: Pipeline Refactor & API Cleanups
**Goal**: Factor the ~2000-line `unified_rag_pipeline` into composable, testable steps with clearer required/optional components, enforced budgets, and cleaner public API boundaries.
**Success Criteria**:
- A `RAGPipelineContext` (or equivalent) internal abstraction exists and is used across phases (retrieval, expansion, caching, guardrails, reranking, generation, claims, post-verification, telemetry).
- The main entry point becomes a thin orchestrator over smaller functions or classes, without changing external behavior.
- Required vs optional modules are clearly separated; production profiles fail loudly when required components are missing, while experimental paths remain opt-in.
- Time/cost budgets are enforced in a centralized way, with sensible defaults per profile.
- API surface issues (e.g., `enable_streaming` semantics, duplicate helpers, return-type guarantees) are documented and partially addressed without breaking existing clients.
**Tests**:
- Existing `tests/RAG_NEW` suites continue to pass.
- New unit tests for at least one extracted phase (e.g., retrieval or guardrails) to validate behavior in isolation.
**Status**: Not Started

## Stage 4: AuthNZ + Resource Governor Long-Tail Alignment
**Goal**: Complete the remaining adoption checklist items after the v1 baseline for Resource Governor and claim-first AuthNZ dependencies.
**Success Criteria**:
- Remaining legacy limiters are retired once RG parity is validated (Chat `ConversationRateLimiter`, Embeddings `UserRateLimiter`, Evaluations/AuthNZ/Character Chat/Web Scraping shims, legacy SlowAPI counters, and non-RG audio concurrency counters).
- Remaining RG-free ingress routes are explicitly documented and either mapped through `route_map` policies or kept out-of-scope with rationale.
- Remaining legacy `require_admin` helpers in evaluations, embeddings v5, and MCP endpoints are migrated to `get_auth_principal` + claims checks (`require_roles("admin")` and/or `require_permissions(...)`).
- New authorization checks stop branching on `is_single_user_mode()` where principal claims should be authoritative.
- Remaining inline AuthNZ SQL/backend detection is moved to repositories (remaining DDL in `initialize.py`, quota counters in `quotas.py`, residual `hasattr(conn, 'fetchval')` paths) while preserving SQLite/Postgres parity.
**Tests**:
- Keep `tests/Resource_Governance/*`, `tests/AuthNZ_Unit/test_auth_principal_*`, `tests/AuthNZ_Unit/*permissions*_claims.py`, `tests/AuthNZ/integration/test_single_user_claims_permissions.py`, and AuthNZ repo tests passing across SQLite/Postgres backends.
**Status**: In Progress
**Progress Notes**:
- 2026-02-08: Removed residual backend detection-by-`hasattr(conn, "fetchval")` in `app/core/AuthNZ/repos/quotas_repo.py`; backend path now keys off `DatabasePool.pool` (`Optional[asyncpg.Pool]` — `None` ⇒ SQLite, non-`None` ⇒ PostgreSQL; repos detect this via `getattr(self.db_pool, "pool", None)`). Added unit coverage in `tests/AuthNZ/unit/test_authnz_quotas_repo_backend_selection.py` to lock SQLite/Postgres path selection behavior. **Backends tested**: SQLite (unit tests) and PostgreSQL (integration; skipped when Postgres unavailable). **Reproduce** (from repo root, `.venv` activated): `python -m pytest -q tldw_Server_API/tests/AuthNZ/unit/test_authnz_quotas_repo_backend_selection.py`.
- 2026-02-08: Removed remaining `hasattr(conn, "fetchval")` backend branching in `app/core/AuthNZ/repos/api_keys_repo.py` and `app/core/AuthNZ/repos/sessions_repo.py`; these paths now also key off `DatabasePool.pool` (same `None`/non-`None` selector described above). **Backends tested**: SQLite (unit) + PostgreSQL (integration, skipped in environments without Postgres). **Reproduce** (from repo root, `.venv` activated): `python -m pytest -q tldw_Server_API/tests/AuthNZ/unit/test_authnz_quotas_repo_backend_selection.py tldw_Server_API/tests/AuthNZ/unit/test_authnz_api_keys_repo_backend_selection.py tldw_Server_API/tests/AuthNZ/unit/test_authnz_sessions_repo_backend_selection.py` (`10 passed, 5 skipped` across SQLite + unit repo suites).
- 2026-02-08: Embeddings policy enforcement now resolves admin bypass claim-first from `request.state.auth` principal (role/is_admin) with compatibility fallback to `User.is_admin`. Updated `/api/v1/embeddings` and `/api/v1/embeddings/batch` policy checks and metrics config reporting to use request-aware evaluation. Added unit coverage in `tests/Embeddings/test_embeddings_policy_claim_first.py`.
- 2026-02-08: Removed the legacy evaluations `require_admin` helper and migrated remaining tests to claim-first `enforce_heavy_evaluations_admin(principal)` checks. Verified with focused suites (`10 passed` for evaluations heavy-admin tests + permissions claims, and `3 skipped` for invariants suite).
- 2026-02-08: MCP endpoint claim-first slice: `list_tool_catalogs` now derives admin visibility from `request.state.auth` `AuthPrincipal` first (is_admin/role `admin`), with token-role fallback only when principal is absent. Added focused unit coverage in `tests/AuthNZ_Unit/test_mcp_tool_catalog_claim_first_admin.py` and verified alongside existing MCP admin permission claims tests (`10 passed`).
- 2026-02-08: Registration service backend branching no longer probes connection capabilities (`hasattr(conn, 'fetchrow'/'fetchval'/'execute')`). `app/services/registration_service.py` now derives backend path from `DatabasePool` state via `_is_postgres_backend()`, including registration-code listing parameter normalization. Added backend-selection unit coverage in `tests/AuthNZ/unit/test_registration_service_backend_selection.py` to ensure SQLite paths remain SQLite even when connection shims expose Postgres-like methods (`3 passed`).
- 2026-02-08: Storage quota service backend branching no longer probes connection capabilities (`hasattr(conn, 'execute'/'fetchrow'/'fetchval')`). `app/services/storage_quota_service.py` now uses `_is_postgres_backend()` derived from `DatabasePool` state for user quota updates and org/team usage recalculation paths. Added backend-selection unit coverage in `tests/AuthNZ/unit/test_storage_quota_service_backend_selection.py` (`3 passed`) and validated existing storage quota suites (`20 passed`).
- 2026-02-08: `UsersDB` backend checks in `app/core/DB_Management/Users_DB.py` now consistently use `_using_postgres_backend()` (DatabasePool-derived) instead of connection capability probing (`hasattr(conn, 'fetchval')`) in `create_user` and `update_user`. Updated backend-detection unit fixtures in `tests/DB_Management/unit/test_users_db_update_backend_detection.py` to set pool backend state explicitly; validated with DB management suites (`2 passed` + `3 passed`).
- 2026-02-08: Billing enforcer backend branching no longer probes connection capabilities (`hasattr(conn, 'fetchval')`) in `_get_api_calls_today`, `_get_llm_tokens_month`, and `_get_storage_bytes`. `app/core/Billing/enforcement.py` now resolves backend from pool state via `_is_postgres_pool(pool)`. Extended `tests/Billing/test_billing_enforcer_org_usage.py` with SQLite trap coverage for API calls and storage bytes plus updated Postgres pool fixtures; validated with billing suites (`5 passed` + `34 passed`).
- 2026-02-08: Single-user bootstrap SQL in `app/core/AuthNZ/initialize.py` no longer performs duplicated Postgres/SQLite inline role/key writes; it now delegates admin-user upsert and admin-role assignment to existing `AuthnzUsersRepo` helpers (`ensure_single_user_admin_user`, `assign_role_if_missing`) and uses `AuthnzApiKeysRepo.upsert_primary_key` for both primary and deterministic test key seeding. Added backend-selection unit coverage in `tests/AuthNZ/unit/test_users_repo_bootstrap_backend_selection.py` and revalidated bootstrap suites (`10 passed` across users-repo backend selection + SQLite bootstrap tests; Postgres bootstrap module executed but `4 skipped` in this environment).
- 2026-02-08: Removed remaining backend routing-by-connection-capability probes in `app/core/AuthNZ/repos/sessions_repo.py` (`hasattr(conn, 'fetchrow'/'fetch')`) and switched all affected session CRUD/refresh/validation/revocation paths to `DatabasePool`-state routing via `_is_postgres_backend()`. Added backend-selection traps in `tests/AuthNZ/unit/test_authnz_sessions_repo_backend_selection.py` and updated `tests/AuthNZ/unit/test_sessions_repo_refresh_gate.py` stubs to set pool backend state explicitly. Validated with focused suites (`8 passed` for unit+SQLite sessions repo coverage; Postgres integration module executed but `2 skipped` in this environment).
- 2026-02-08: Removed remaining backend routing-by-connection-capability probe in `app/core/AuthNZ/repos/registration_codes_repo.py` (`hasattr(conn, 'fetchrow')`) and switched deactivation cleanup path to `_is_postgres_backend()` (DatabasePool state). Added backend-selection unit coverage in `tests/AuthNZ/unit/test_authnz_registration_codes_repo_backend_selection.py` and revalidated combined AuthNZ repo slices (`10 passed` across registration-codes + sessions backend-selection and SQLite sessions repo suites).
- 2026-02-08: Removed remaining backend routing-by-connection-capability probes in `app/core/AuthNZ/repos/usage_repo.py` prune helpers (`hasattr(conn, 'fetchrow')`) and switched all four prune paths to `_is_postgres_backend()` (DatabasePool state). Expanded `tests/AuthNZ/unit/test_authnz_usage_repo_backend_selection.py` to cover SQLite/Postgres routing for `prune_usage_log_before`, `prune_llm_usage_log_before`, `prune_usage_daily_before`, and `prune_llm_usage_daily_before`; validated with focused suites (`9 passed, 1 skipped` across usage backend-selection/unit+sqlite+postgres modules in this environment).
- 2026-02-08: Removed backend routing-by-connection-capability probing in `app/core/AuthNZ/repos/mfa_repo.py` (`hasattr(conn, 'fetchrow')`) and switched MFA repo routing to `DatabasePool` backend state via `_is_postgres_backend()`. Added targeted backend-selection coverage in `tests/AuthNZ/unit/test_authnz_mfa_repo_backend_selection.py` for transaction and acquire paths (`set_mfa_config`, `get_mfa_status_row`, `get_encrypted_totp_secret`) and validated with MFA-focused suites (`7 passed, 1 skipped` across unit+sqlite+postgres repo modules in this environment).
- 2026-02-08: Removed backend routing-by-connection-capability probes in `app/core/AuthNZ/repos/monitoring_repo.py` (`hasattr(conn, 'fetchrow')`) and normalized backend selection to `_is_postgres_backend()` (DatabasePool state) across insert/delete and read-query helpers. Added backend-selection trap coverage in `tests/AuthNZ/unit/test_authnz_monitoring_repo_backend_selection.py` for `insert_metric_audit_log` and `delete_audit_logs_before`; validated with monitoring-focused suites (`5 passed, 1 skipped` across unit+sqlite+postgres modules in this environment).
- 2026-02-08: Removed the remaining `hasattr(conn, 'fetchrow')` branch in `app/core/AuthNZ/repos/generated_files_repo.py` (`create_file`) and aligned it with pool-state backend routing via `_is_postgres()`. Added backend-selection trap coverage in `tests/AuthNZ/unit/test_authnz_generated_files_repo_backend_selection.py` to assert SQLite `execute` and Postgres `fetchrow` paths; validated with focused unit run (`2 passed`).
- 2026-02-08: Removed connection-capability backend probing in `app/core/AuthNZ/repos/org_invites_repo.py` by updating `_is_postgres(...)` to rely on `DatabasePool` state (and keeping `conn` arg only for compatibility with existing call sites). Added backend-selection trap coverage in `tests/AuthNZ/unit/test_authnz_org_invites_repo_backend_selection.py` for `create_invite` SQLite/Postgres paths, and validated with related billing invite coverage (`34 passed` across new unit tests + `tests/Billing/test_org_invite_service.py`).
- 2026-02-08: Removed backend routing-by-connection-capability probes from `app/core/AuthNZ/repos/token_blacklist_repo.py` (`hasattr(conn, 'fetchrow')`) by introducing `_is_postgres_backend()` and routing insert/query/cleanup/stats paths from `DatabasePool` state. Added backend-selection trap coverage in `tests/AuthNZ/unit/test_authnz_token_blacklist_repo_backend_selection.py` and validated with focused blacklist suites (`7 passed, 1 skipped` across new unit + SQLite + Postgres repo + counts tests in this environment).
- 2026-02-08: Removed backend routing-by-connection-capability probes from `app/core/AuthNZ/repos/rate_limits_repo.py` (`hasattr(conn, 'fetchrow')`) and standardized backend routing on `_is_postgres_backend()` (DatabasePool state) across schema/bootstrap, bucket increment/delete, lockout upsert/read/reset, and cleanup paths. Added backend-selection trap coverage in `tests/AuthNZ/unit/test_authnz_rate_limits_repo_backend_selection.py`; validated with focused rate-limit suites (`11 passed, 2 skipped` across new unit + SQLite/Postgres repo + lockout/bootstrap unit tests in this environment).
- 2026-02-08: Removed remaining `hasattr(conn, 'fetchrow')` backend probes from `app/core/AuthNZ/repos/api_keys_repo.py` in rotation/revocation/usage/expiry/audit helpers and routed those branches through existing `_is_postgres_backend()` (DatabasePool state). Added backend-selection trap coverage in `tests/AuthNZ/unit/test_authnz_api_keys_repo_backend_selection.py` and updated `tests/AuthNZ_Unit/test_authnz_api_keys_repo_status_parsing.py` fixture pool markers for claim-preserving Postgres-path parsing coverage. Validated with focused api-key suites (`7 passed, 7 skipped` across backend-selection/status-parsing/SQLite/Postgres repo modules in this environment).
- 2026-02-08: Removed remaining connection-capability backend probes from `app/core/AuthNZ/repos/orgs_teams_repo.py` (`hasattr(conn, 'fetchrow')`, `hasattr(conn, 'execute')`) by making `_is_postgres(...)` pool-state authoritative and routing all affected org/team membership + default-team helper branches through that backend selector. Added backend-selection trap coverage in `tests/AuthNZ/unit/test_authnz_orgs_teams_repo_backend_selection.py` and validated with focused org/team suites (`6 passed, 2 skipped` across new unit + SQLite + Postgres repo modules in this environment).
- 2026-02-08: Finalized the repository-side backend-probe cleanup sweep by removing the remaining docstring reference to connection-method probing in `app/core/AuthNZ/repos/quotas_repo.py`; grep verification now reports no live `hasattr(conn, ...)` backend-routing patterns under `app/core/AuthNZ/repos`.
- 2026-02-08: Removed the remaining runtime connection-probe in `app/core/AuthNZ/scheduler.py` (`_prune_privilege_snapshots` branch selector now derives Postgres/SQLite from `DatabasePool` state). Repo + AuthNZ-core grep verification now reports no live `hasattr(conn, ...)` backend-routing patterns under `app/core/AuthNZ`.
- 2026-02-08: Fixed scheduler-workflows claims tests by stabilizing dependency override targets for `/api/v1/scheduler/workflows/admin/rescan`: extracted named dependency callables in `app/api/v1/endpoints/scheduler_workflows.py` (`_ADMIN_RESCAN_SCOPE_DEP`, `_ADMIN_RESCAN_PERMISSIONS_DEP`) and updated `tests/AuthNZ_Unit/test_scheduler_workflows_permissions_claims.py` to override the exact scope dependency object (plus zero-arg fake dependency signature). Verified with focused test run (`4 passed`).
- 2026-02-08: Added a static backend-detection guardrail in `tests/AuthNZ/unit/test_backend_detection_guardrail.py` to prevent reintroducing connection-capability backend routing (`hasattr(conn, 'fetch/fetchrow/fetchval/execute')`) inside `app/core/AuthNZ`. Verified alongside scheduler claims permissions tests (`5 passed` combined).
- 2026-02-08: Added a claim-first API guardrail in `tests/AuthNZ_Unit/test_claim_first_single_user_mode_guardrail.py` to prevent non-allowlisted `is_single_user_mode()` authorization branching in `app/api/v1` (allowlisting only the explicit compatibility fallback in `API_Deps/auth_deps.py`). Verified together with scheduler + backend guardrails (`6 passed` combined).
- 2026-02-08: Removed the remaining runtime backend-capability probe in `app/services/storage_quota_service.py` (`get_all_users_storage` no longer branches on `hasattr(conn, 'fetch')`; it now uses `_is_postgres_backend()`/`DatabasePool` state). Extended `tests/AuthNZ/unit/test_storage_quota_service_backend_selection.py` with SQLite/Postgres trap coverage for `get_all_users_storage`; validated with focused guardrail + storage suites (`11 passed`).
- 2026-02-08: Hardened claim-first fallback semantics in `app/core/AuthNZ/auth_principal_resolver.py` by making `_peek_jwt_token_type(...)` fail-open on malformed JWT segments (returns `None` instead of bubbling decode errors), so mixed-credential requests can still fall back to API-key auth when JWT validation fails. Added verification via `tests/AuthNZ_Unit/test_auth_principal_resolver.py`.
- 2026-02-08: Stabilized claim permission unit suites affected by dynamic dependency factories: `tests/AuthNZ_Unit/test_notes_graph_permissions_claims.py` now no-ops per-route token-scope/rate-limit dependencies by metadata (`_tldw_token_scope`/`_tldw_rate_limit_resource`) so tests isolate `require_permissions(...)` behavior; `tests/AuthNZ_Unit/test_vector_stores_permissions_claims.py` now monkeypatches the module-level `_get_adapter_for_user` helper (non-Depends call-site) for admin-health coverage.
- 2026-02-08: Revalidated the Stage 4 claims-focused regression command after fixes: `python -m pytest -q tests/AuthNZ_Unit/test_auth_principal_* tests/AuthNZ_Unit/*permissions*_claims.py tests/AuthNZ/integration/test_single_user_claims_permissions.py` => `87 passed, 3 skipped`.
- 2026-02-09: Continued Stage 4 long-tail sweep across MCP scope endpoints and endpoint/service backend-selection leftovers. Migrated `app/api/v1/endpoints/mcp_catalogs_manage.py` from `get_current_user` user-dict gating to claim-first `AuthPrincipal` dependencies (`get_auth_principal`) with principal-based admin bypass and manager checks (`principal.user_id` + org/team membership roles). Removed remaining inline connection-capability backend probes in `app/services/admin_orgs_service.py` (`update_org_watchlists_settings`, `get_org_watchlists_settings`) and `app/api/v1/endpoints/orgs.py` (organization delete/transfer, team update/delete paths) by routing through explicit backend selectors (`is_postgres_backend()`) instead of `hasattr(..., 'fetchrow')`. Added focused coverage in `tests/AuthNZ_Unit/test_mcp_catalogs_manage_claim_first.py` and `tests/AuthNZ/unit/test_admin_orgs_service_backend_selection.py`, and revalidated with `python -m pytest -q tldw_Server_API/tests/AuthNZ_Unit/test_mcp_catalogs_manage_claim_first.py tldw_Server_API/tests/AuthNZ/unit/test_admin_orgs_service_backend_selection.py tldw_Server_API/tests/Admin/test_admin_watchlists_org_settings.py` (`9 passed`).
- 2026-02-09: Continued Stage 4 backend-selection cleanup by removing connection-capability probing from the AuthNZ test DB adapter path in `app/api/v1/API_Deps/auth_deps.py::get_db_transaction` (adapter backend now keys off pool state via `db_pool.pool`, not `hasattr(conn, "fetchrow")`). Added regression coverage in `tests/AuthNZ_Unit/test_auth_deps_db_adapter_backend_selection.py` to assert both PostgreSQL and SQLite adapter behavior is selected from pool state even when connection capabilities are misleading. Revalidated with focused suites (`42 passed`): `TLDW_CONFIG_FILE=/tmp/tldw_min_config_stage4.ini python -m pytest -q tldw_Server_API/tests/AuthNZ/unit/test_admin_usage_service_backend_selection.py tldw_Server_API/tests/AuthNZ/unit/test_password_service_backend_selection.py tldw_Server_API/tests/AuthNZ_Unit/test_auth_deps_db_adapter_backend_selection.py tldw_Server_API/tests/AuthNZ_Unit/test_auth_deps_hardening.py tldw_Server_API/tests/AuthNZ/unit/test_security_guards.py`.
- 2026-02-09: Continued Stage 4 claim-first migration in `app/api/v1/endpoints/audit.py` by replacing shared-storage cross-tenant admin gating (`current_user.is_admin`) with principal-first checks (`get_auth_principal` + `AuthPrincipal` admin claims/roles). Non-admin shared-mode scoping now resolves tenant filter from principal user id (`principal.user_id`) with compatibility fallback to request user id. Added precedence coverage in `tests/Audit/test_audit_export_endpoint.py` (`test_audit_shared_mode_uses_principal_not_user_admin_for_cross_tenant`, `test_audit_shared_mode_allows_principal_admin_even_if_user_not_admin`) and revalidated the full audit export/count endpoint suite (`19 passed`): `TLDW_CONFIG_FILE=/tmp/tldw_min_config_stage4.ini python -m pytest -q tldw_Server_API/tests/Audit/test_audit_export_endpoint.py`.
- 2026-02-09: Continued Stage 4 inline AuthNZ SQL/backend cleanup in `app/api/v1/endpoints/mcp_unified_endpoint.py::list_tool_catalogs` by replacing the endpoint-local `team_members` query (and per-backend SQL branching) with repository-backed membership lookup (`list_active_team_memberships_for_user(...)`). Added repo helper support in `app/core/AuthNZ/repos/orgs_teams_repo.py` and `app/core/AuthNZ/orgs_teams.py` to keep backend-specific SQL in the AuthNZ repository layer with explicit active-membership filtering (`status='active'`), and added focused coverage in `tests/AuthNZ_Unit/test_mcp_tool_catalog_claim_first_admin.py` + `tests/AuthNZ/unit/test_authnz_orgs_teams_repo_backend_selection.py`. Revalidated with: `TLDW_CONFIG_FILE=/tmp/tldw_min_config_stage4.ini python -m pytest -q tldw_Server_API/tests/AuthNZ_Unit/test_mcp_tool_catalog_claim_first_admin.py tldw_Server_API/tests/AuthNZ/unit/test_authnz_orgs_teams_repo_backend_selection.py tldw_Server_API/tests/AuthNZ_Unit/test_mcp_admin_permissions_claims.py` (`17 passed`).
- 2026-02-09: Continued Stage 4 endpoint inline-SQL cleanup in `app/api/v1/endpoints/mcp_catalogs_manage.py` by removing per-endpoint PostgreSQL/SQLite branching (`is_postgres_backend()` + raw SQL) from org/team catalog CRUD and entry mutation routes. Scope routes now use claim-first manager checks plus service/repository operations (`app/services/admin_tool_catalog_service.py`) with explicit scope ownership validation (`_get_scoped_catalog`) and paged scoped listing helper (`_list_scoped_catalogs`). Added focused coverage for scope/helper and conflict mapping in `tests/AuthNZ_Unit/test_mcp_catalogs_manage_claim_first.py` and revalidated MCP/AuthNZ catalog suites with: `TLDW_CONFIG_FILE=/tmp/tldw_min_config_stage4.ini python -m pytest -q tldw_Server_API/tests/AuthNZ_Unit/test_mcp_catalogs_manage_claim_first.py tldw_Server_API/tests/AuthNZ_Unit/test_mcp_admin_permissions_claims.py tldw_Server_API/tests/AuthNZ_Unit/test_mcp_tool_catalog_claim_first_admin.py tldw_Server_API/tests/Services/test_admin_tool_catalog_service_sqlite.py` (`20 passed`).
- 2026-02-09: Continued Stage 4 endpoint/backend cleanup in `app/api/v1/endpoints/mcp_unified_endpoint.py::list_tool_catalogs` by moving remaining catalog visibility SQL branching (`is_postgres_backend` + `IN` placeholder logic) into `app/services/admin_tool_catalog_service.py::list_visible_tool_catalogs(...)`. The endpoint now delegates visibility retrieval to the service and keeps only claim/scope assembly + response normalization; backend-specific query details are centralized in service/repo layer. Added SQLite coverage for scope visibility filtering in `tests/Services/test_admin_tool_catalog_service_sqlite.py::test_list_visible_tool_catalogs_sqlite_scope_filters` and updated MCP claim-first unit coverage in `tests/AuthNZ_Unit/test_mcp_tool_catalog_claim_first_admin.py` to assert service delegation + scoped membership inputs. Revalidated with: `TLDW_CONFIG_FILE=/tmp/tldw_min_config_stage4.ini python -m pytest -q tldw_Server_API/tests/AuthNZ_Unit/test_mcp_tool_catalog_claim_first_admin.py tldw_Server_API/tests/AuthNZ_Unit/test_mcp_admin_permissions_claims.py tldw_Server_API/tests/AuthNZ_Unit/test_mcp_catalogs_manage_claim_first.py tldw_Server_API/tests/Services/test_admin_tool_catalog_service_sqlite.py` (`21 passed`).
- 2026-02-09: Continued Stage 4 endpoint/backend cleanup in `app/api/v1/endpoints/orgs.py` by removing the remaining endpoint-local PostgreSQL/SQLite branching (`is_postgres_backend()`) from org delete/ownership-transfer and team update/delete paths. Added repository helpers in `app/core/AuthNZ/repos/orgs_teams_repo.py` (`delete_organization_with_provider_secrets`, `transfer_organization_ownership`, `update_team`, `delete_team_with_provider_secrets`) so backend-specific SQL stays in AuthNZ repo layer, and updated the org endpoints to delegate through those helpers. Added backend-selection trap coverage in `tests/AuthNZ/unit/test_authnz_orgs_teams_repo_backend_selection.py` for the new methods and revalidated with: `source .venv/bin/activate && python -m pytest -q tldw_Server_API/tests/AuthNZ/unit/test_authnz_orgs_teams_repo_backend_selection.py` (`11 passed`).
- 2026-02-09: Continued Stage 4 endpoint/backend cleanup in `app/api/v1/endpoints/users.py` by removing endpoint-local backend branching for password-hash lookup in `/api/v1/users/change-password`. The route now resolves user data through `AuthnzUsersRepo` (`from_pool` + `get_user_by_id`) so backend SQL remains encapsulated in AuthNZ repository/database layers. Updated unit endpoint tests to mock repo resolution (`tests/AuthNZ/unit/test_user_endpoints.py`) instead of backend probe monkeypatching, and revalidated with: `source .venv/bin/activate && python -m pytest -q tldw_Server_API/tests/AuthNZ/unit/test_authnz_orgs_teams_repo_backend_selection.py tldw_Server_API/tests/AuthNZ/unit/test_user_endpoints.py -k "change_password or orgs_teams_repo_backend_selection"` (`11 passed, 2 skipped, 5 deselected`).
- 2026-02-09: Continued Stage 4 Auth endpoint inline-backend cleanup in `app/api/v1/endpoints/auth.py` by extracting the remaining forgot/reset/verify/resend/magic-link PostgreSQL-vs-SQLite SQL branches into dedicated private DB helpers (`_fetch_user_by_email_for_password_reset`, `_store_password_reset_token`, `_fetch_password_reset_token_record`, `_apply_password_reset`, `_verify_user_email_once`, `_fetch_user_by_email_for_verification`, `_mark_user_verified`). Endpoint handlers no longer embed backend-branching SQL blocks directly for those flows; they delegate to helper calls while preserving existing MFA PostgreSQL feature-gating behavior. Updated unit coverage in `tests/AuthNZ/unit/test_auth_endpoints_extended.py` to keep reset-password tests fully unit-isolated (stubbed token blacklist). Revalidated with: `source .venv/bin/activate && python -m pytest -q tldw_Server_API/tests/AuthNZ/unit/test_auth_endpoints_extended.py -k "reset_password_weak_and_success or forgot_password_rate_limited_returns_generic_message or reset_password_rate_limited_returns_429"` (`3 passed, 19 deselected`). Attempted `tests/AuthNZ/integration/test_forgot_password_flow_integration.py`; it failed in this environment during app startup due local SQLite initialization error (`sqlite3.DatabaseError: file is not a database`), not due endpoint assertion drift.
- 2026-02-09: Continued Stage 4 inline backend-detection cleanup by removing `is_postgres_backend()` branching from `app/services/auth_service.py` (`fetch_user_by_login_identifier`, `update_user_password_hash`, `update_user_last_login`, `fetch_active_user_by_id`). Auth service DB access now uses backend-agnostic adapter calls (`fetchrow`/`execute` with sqlite compatibility fallbacks) so selection is no longer hard-coded in endpoint/service logic. Added focused unit coverage in `tests/AuthNZ/unit/test_auth_service_backend_agnostic.py` and a claim-first guardrail in `tests/AuthNZ/unit/test_require_admin_callsite_guardrail.py` to prevent reintroducing endpoint `Depends(require_admin)` call sites. Revalidated with: `source .venv/bin/activate && python -m pytest -q tldw_Server_API/tests/AuthNZ/unit/test_auth_service_backend_agnostic.py tldw_Server_API/tests/AuthNZ/unit/test_require_admin_callsite_guardrail.py` (`6 passed`) and a focused auth endpoint regression slice (`python -m pytest -q tldw_Server_API/tests/AuthNZ/unit/test_auth_endpoints_extended.py -k "login_invalid_password or reset_password_weak_and_success or refresh_token_missing_fields_422"` => `1 passed, 21 deselected`).
- 2026-02-09: Continued Stage 4 MFA claim/backend cleanup by moving auth endpoint MFA backend support detection behind MFA service capability (`MFAService.supports_backend`) and adding endpoint helper `_is_mfa_backend_supported()` in `app/api/v1/endpoints/auth.py` (service-first, legacy backend-probe fallback for test stubs). Updated MFA availability/login gating in auth endpoints to use this helper instead of direct per-call backend checks in the login flow. Added focused unit coverage in `tests/AuthNZ/unit/test_auth_endpoints_extended.py::test_is_mfa_backend_supported_prefers_mfa_service_capability`, and revalidated MFA/auth slices with `python -m pytest -q tldw_Server_API/tests/AuthNZ/unit/test_auth_endpoints_extended.py -k "is_mfa_backend_supported_prefers_mfa_service_capability or login_returns_mfa_challenge_when_enabled or setup_mfa_accepts_dict_current_user or verify_mfa_setup_succeeds_when_email_send_fails"` (`4 passed, 19 deselected`).
- 2026-02-09: Continued Stage 4 auth endpoint inline-SQL cleanup by removing the remaining password-reset and email-verification backend branches from `app/api/v1/endpoints/auth.py` helpers (`_fetch_user_by_email_for_password_reset`, `_store_password_reset_token`, `_fetch_password_reset_token_record`, `_apply_password_reset`, `_verify_user_email_once`, `_fetch_user_by_email_for_verification`, `_mark_user_verified`). These helpers now delegate to `app/services/auth_service.py` operations, which encapsulate backend adapter compatibility (`fetchrow`/`execute` + placeholder normalization) outside endpoint code. Added service-layer coverage in `tests/AuthNZ/unit/test_auth_service_backend_agnostic.py` for reset-token insert/lookup and verify-email update-count handling; revalidated with `python -m pytest -q tldw_Server_API/tests/AuthNZ/unit/test_auth_service_backend_agnostic.py` (`8 passed`) and focused auth endpoint reset/verification slice `python -m pytest -q tldw_Server_API/tests/AuthNZ/unit/test_auth_endpoints_extended.py -k "reset_password_weak_and_success or forgot_password_rate_limited_returns_generic_message or verify_email or resend_verification"` (`2 passed, 21 deselected`).
- 2026-02-09: Continued Stage 4 auth endpoint backend-detection cleanup by removing the final runtime `is_postgres_backend()` call path in `app/api/v1/endpoints/auth.py` (`_is_mfa_backend_supported` now resolves MFA support only via `MFAService.supports_backend()` and returns unsupported when the capability is absent). Updated MFA endpoint unit stubs in `tests/AuthNZ/unit/test_auth_endpoints_extended.py` to patch `_is_mfa_backend_supported()` directly (instead of backend probe monkeypatches), preserving claim-first/service-capability semantics under test. Revalidated with: `python -m pytest -q tldw_Server_API/tests/AuthNZ/unit/test_auth_endpoints_extended.py -k "is_mfa_backend_supported_prefers_mfa_service_capability or login_returns_mfa_challenge_when_enabled or mfa_login_completes_tokens or setup_mfa_accepts_dict_current_user or verify_mfa_setup_succeeds_when_email_send_fails or verify_mfa_setup_rate_limited_returns_429 or mfa_login_rate_limited_returns_429"` (`7 passed, 16 deselected`) plus focused bundle `python -m pytest -q tldw_Server_API/tests/AuthNZ/unit/test_auth_service_backend_agnostic.py tldw_Server_API/tests/AuthNZ/unit/test_auth_endpoints_extended.py -k "reset_password_weak_and_success or forgot_password_rate_limited_returns_generic_message or is_mfa_backend_supported_prefers_mfa_service_capability or login_returns_mfa_challenge_when_enabled or mfa_login_completes_tokens or setup_mfa_accepts_dict_current_user or verify_mfa_setup_succeeds_when_email_send_fails or verify_mfa_setup_rate_limited_returns_429 or mfa_login_rate_limited_returns_429" tldw_Server_API/tests/AuthNZ/unit/test_require_admin_callsite_guardrail.py` (`9 passed, 23 deselected`).
- 2026-02-09: Continued Stage 4 backend-detection and claim-first guardrail cleanup by removing global backend probing from `app/core/AuthNZ/byok_rotation.py` (`rotate_byok_secrets(...)` now derives PostgreSQL/SQLite from the provided `DatabasePool` state via `_is_postgres_pool(...)` instead of calling `is_postgres_backend()`). Added backend-selection unit coverage in `tests/AuthNZ/unit/test_byok_rotation_backend_selection.py` to assert pool-state routing for both SQLite and PostgreSQL paths. Tightened `tests/AuthNZ/unit/test_require_admin_callsite_guardrail.py` to fail on legacy endpoint imports/usages of `require_admin` (not only `Depends(require_admin)` patterns). While validating this slice, fixed a latent AES-GCM exception tuple bug in `app/core/Security/crypto.py::decrypt_json_blob_with_key` that surfaced during BYOK key-rotation tests. Revalidated with: `python -m pytest -q tldw_Server_API/tests/AuthNZ/unit/test_byok_rotation_backend_selection.py tldw_Server_API/tests/AuthNZ_SQLite/test_byok_rotation_sqlite.py tldw_Server_API/tests/AuthNZ/unit/test_require_admin_callsite_guardrail.py` (`4 passed`).

## Stage 5: RG v1.1 Legacy Limiter Retirement
**Goal**: Safely retire remaining legacy limiters/shims once RG parity is verified, leaving ResourceGovernor as the sole enforcer.
**Success Criteria**:
- Per-module shadow mismatch metrics show near-zero drift for at least one release window on representative traffic.
- All mapped routes return stable 429/`Retry-After`/`X-RateLimit-*` headers under both memory and Redis backends.
- Legacy limiters are demoted to diagnostics-only shims (no counters) with deprecation warnings, then removed after one stable release.
- Unused `RG_ENABLE_*` aliases and legacy flags are deleted post-release and docs/examples reference RG policies/envs only.
**Tests**:
- For each module (chat, embeddings, authnz, evals, character-chat, web-scraping, audio, workflows), keep/extend parity tests and remove legacy-path assertions only after cutover.
- Regression suite ensures no route double-enforces after shim removal.
**Status**: In Progress
**Progress Notes**:
- 2026-02-08: Extended `tests/Resource_Governance/test_e2e_evals_authnz_character_headers.py` to run deny-header parity coverage across both `RG_BACKEND=memory` and `RG_BACKEND=redis` (8 parametrized cases). Each case now uses backend-scoped policy IDs to reduce cross-test key collisions when Redis stub state is reused.
- 2026-02-08: Extended `tests/Resource_Governance/test_e2e_domains_headers.py` to run deny-header parity coverage across both `RG_BACKEND=memory` and `RG_BACKEND=redis` (8 parametrized cases) for rag/media/research/prompt-studio routes, with backend-scoped policy IDs for Redis-stub isolation.
- 2026-02-08: Extended `tests/Resource_Governance/test_e2e_chat_audio_headers.py` to run both backends (14 parametrized cases) across chat, embeddings, mcp, audio websocket, and audio transcription header paths. Updated the audio transcription test to inject an `sf` shim via `audio` module patch-point (instead of assuming `audio_ep.sf` exists) for compatibility with current endpoint exports. Verified end-to-end with combined run: `30 passed` across the three updated E2E modules.
- 2026-02-08: Extended `tests/Resource_Governance/test_e2e_tokens_daily_cap.py` (chat + embeddings tokens daily-cap denial) and `tests/Resource_Governance/test_e2e_workflows_daily_cap.py` (workflows daily-cap denial) to run under both `RG_BACKEND=memory` and `RG_BACKEND=redis`, with backend-scoped policy IDs. Verified combined RG E2E parity set: `36 passed` across evals/authnz/character, domains, chat/audio, tokens, and workflows modules.
- 2026-02-08: Revalidated Stage 5 cutover/bypass/shadow parity after Stage 4 claim-first fixes: `tests/Resource_Governance/test_rg_cutover_*`, `test_chat_rg_limiter_cutover.py`, `test_rg_single_source_bypass.py`, and `test_rg_shadow_metrics.py` all pass (`16 passed`). Route-map coverage checks also pass (`tests/Resource_Governance/test_slowapi_decorated_routes_mapped.py`, `test_auth_route_map_coverage.py`: `2 passed`).
- 2026-02-08: Ran the full Resource Governance regression package to baseline RG v1.1 retirement readiness: `python -m pytest -q tldw_Server_API/tests/Resource_Governance` => `126 passed, 14 skipped, 2 xfailed` in this environment (Postgres/real-Redis integration skips expected).
- 2026-02-08: Started Stage 5 chat-first limiter retirement by demoting `ConversationRateLimiter` RG-enabled fallback to diagnostics-only (no legacy token/counter mutation) when RG is enabled but unavailable. Added non-mutating legacy peek checks and one-shot deprecation diagnostics in `app/core/Chat/rate_limiter.py`, while preserving legacy enforcement when `RG_ENABLED=0` and preserving explicit RG deny semantics. Added cutover regression coverage in `tests/Resource_Governance/test_chat_rg_limiter_cutover.py::test_chat_rate_limiter_rg_unavailable_uses_diagnostics_only_shim`; validated with `test_chat_rg_limiter_cutover.py` + `test_rg_single_source_bypass.py` (`5 passed`) and the broader cutover bundle (`17 passed`).
- 2026-02-08: Continued Stage 5 limiter retirement for embeddings by demoting `AsyncRateLimiter` RG-enabled fallback to diagnostics-only (non-mutating legacy peek + allow) when RG is enabled but unavailable. Added `UserRateLimiter.peek_rate_limit_non_mutating(...)`, one-shot diagnostics logging, and cutover regression coverage in `tests/Resource_Governance/test_rg_cutover_embeddings_mcp.py::test_embeddings_rg_unavailable_uses_diagnostics_only_shim`. Validated with focused embeddings cutover (`4 passed`) and broader cutover bundle (`18 passed`).
- 2026-02-08: Continued Stage 5 limiter retirement for evaluations by demoting `UserRateLimiter` RG-enabled unavailable path to diagnostics-only (non-mutating legacy checks + allow, no `_record_request` counter writes). Added one-shot diagnostics logging in `app/core/Evaluations/user_rate_limiter.py` and coverage in `tests/Resource_Governance/test_rg_cutover_evals_authnz_character_web.py::test_evaluations_rg_unavailable_uses_diagnostics_only_shim`. Revalidated focused eval/authnz/character/web cutover (`7 passed`) and broader cutover bundle (`19 passed`).
- 2026-02-08: Continued Stage 5 limiter retirement for web scraping by demoting `RateLimiter.acquire()` legacy sleeps/counter mutation to diagnostics-only whenever `RG_ENABLED=1` (including RG-unavailable path). Added one-shot fallback/diagnostic warnings in `app/core/Web_Scraping/enhanced_web_scraping.py` and cutover coverage in `tests/Resource_Governance/test_rg_cutover_evals_authnz_character_web.py::test_web_scraping_rg_unavailable_uses_diagnostics_only_shim` (asserts no legacy sleep path and no request counter mutation). Revalidated focused eval/authnz/character/web cutover (`8 passed`) and broader cutover bundle (`20 passed`).
- 2026-02-08: Re-audited AuthNZ + Character Chat RG-enabled paths during the same cutover sweep; both are already RG-first/non-enforcing on RG-unavailable path (`check_rate_limit` allow/no-op semantics), so no code changes were required for those modules in this slice.
- 2026-02-08: Continued Stage 5 limiter retirement for audio concurrency by removing dead legacy in-process counters from `app/core/Usage/audio_quota.py`, aligning RG-unavailable log semantics to diagnostics-only shim behavior (no legacy enforcement/counters), and adding explicit cutover coverage in `tests/Resource_Governance/test_rg_cutover_audio_quota.py` to assert RG-enabled + unavailable stream/job paths allow without mutating handle/counter state. Revalidated with focused audio suites (`8 passed`) and the broader cutover bundle (`22 passed`).
- 2026-02-08: Continued Stage 5 limiter retirement for workflows daily-cap enforcement by demoting the RG-policy-governed fallback path in `app/api/v1/endpoints/workflows.py::_enforce_workflows_daily_cap` to diagnostics-only when the governor is unavailable or check execution fails (no direct `check_daily_cap` fallback enforcement in this branch). Added coverage in `tests/Resource_Governance/test_rg_cutover_workflows_quota.py` for both unavailable and check-failure paths, and revalidated with workflows-focused suites (`4 passed`, including `test_e2e_workflows_daily_cap.py`) plus the broader cutover bundle (`24 passed`).
- 2026-02-08: Continued Stage 5 legacy-flag cleanup by removing the now-unused `AUDIO_QUOTA_USE_REDIS` compatibility path from `app/core/Usage/audio_quota.py` (Redis/in-process audio counters are retired; audio concurrency is RG-handle based). Updated environment variable docs in `Docs/Operations/Env_Vars.md` and `Docs/Published/Env_Vars.md` to remove the obsolete flag and point `REDIS_URL` guidance at RG (`RG_BACKEND=redis`).
- 2026-02-08: Continued Stage 5 workflows cutover by removing RG-enabled enforcement of the legacy env daily-cap fallback (`WORKFLOWS_QUOTA_DAILY_PER_USER`) in `app/api/v1/endpoints/workflows.py::_enforce_workflows_daily_cap`; when RG is enabled and no policy daily cap exists, the path is now diagnostics-only and non-enforcing (`missing_rg_daily_cap_policy`) to preserve RG single-source semantics. Added cutover coverage in `tests/Resource_Governance/test_rg_cutover_workflows_quota.py::test_workflows_daily_cap_rg_enabled_without_policy_skips_legacy_env_fallback` and revalidated workflows + broader cutover suites (`5 passed` workflows-focused; `25 passed` broader cutover bundle).
- 2026-02-08: Revalidated the full Resource Governance regression suite after the workflows RG single-source cutover: `python -m pytest -q tldw_Server_API/tests/Resource_Governance` => `135 passed, 14 skipped, 2 xfailed` in this environment.
- 2026-02-08: Completed the workflows daily-cap legacy fallback retirement by removing `WORKFLOWS_QUOTA_DAILY_PER_USER` enforcement and `check_daily_cap(...)` fallback wiring from `app/api/v1/endpoints/workflows.py::_enforce_workflows_daily_cap`; daily caps are now RG-policy-only (`workflows_runs.daily_cap`) and missing policy cap paths are diagnostics-only (`missing_rg_daily_cap_policy`). Updated `/api/v1/workflows/config` to expose `quota_daily_cap_source` instead of env-backed `quota_daily_per_user`, and aligned `Docs/Design/Workflows.md` to remove legacy env-cap references.
- 2026-02-08: Revalidated the full Resource Governance regression suite again after simplifying workflows daily-cap control flow (removing now-redundant `if daily_cap_policy > 0` wrapper while keeping behavior unchanged): `python -m pytest -q tldw_Server_API/tests/Resource_Governance` => `135 passed, 14 skipped, 2 xfailed` in this environment.
- 2026-02-08: Continued Stage 5 workflows legacy-flag retirement by removing residual `WORKFLOWS_QUOTA_BURST_PER_MIN` and `WORKFLOWS_DISABLE_RATE_LIMITS` compatibility plumbing from Workflows surfaces: `/api/v1/workflows/config` now reports `ingress_source="rg_policy.route_map+requests"` (no env-backed burst field), and startup preflight test-toggle warnings no longer reference `WORKFLOWS_DISABLE_RATE_LIMITS`. Updated `Docs/Design/Workflows.md` to describe burst ingress control via RG policy `requests` instead of legacy env toggles.
- 2026-02-08: Revalidated the full Resource Governance regression suite after removing residual workflows burst/rate-limit compatibility flags: `python -m pytest -q tldw_Server_API/tests/Resource_Governance` => `135 passed, 14 skipped, 2 xfailed` in this environment (plus focused workflows + cutover runs remained green: `5 passed` and `25 passed`).
- 2026-02-08: Continued Stage 5 limiter-shim cleanup by removing residual diagnostics-only legacy simulation paths from Chat and Embeddings limiters: deleted chat legacy bucket simulation helpers (`_bucket_has_capacity`, `ConversationRateLimiter._peek_legacy_rate_limit`, and one-shot legacy diagnostic logging) and embeddings non-mutating legacy peek helpers (`UserRateLimiter.peek_rate_limit_non_mutating`, `AsyncRateLimiter._peek_legacy_rate_limit`, and one-shot legacy diagnostic logging). RG-enabled + RG-unavailable behavior now logs fallback once and allows without executing legacy simulation logic. Revalidated with focused cutover suites (`7 passed`), broader cutover bundle (`25 passed`), and full RG regression (`135 passed, 14 skipped, 2 xfailed`).
- 2026-02-08: Continued Stage 5 limiter-shim cleanup by removing residual diagnostics-only legacy simulation paths from Evaluations and Web Scraping: `UserRateLimiter.check_rate_limit` RG-unavailable branch now returns allow/fallback metadata without running legacy minute/daily/cost checks, and web scraping `RateLimiter.acquire` no longer computes legacy wait diagnostics under RG-active paths. Updated `tests/Resource_Governance/test_rg_cutover_evals_authnz_character_web.py::test_evaluations_rg_unavailable_uses_diagnostics_only_shim` to assert legacy checks/counter writes are not invoked. Revalidated focused cutover (`8 passed`), broader cutover bundle (`25 passed`), and full RG regression (`135 passed, 14 skipped, 2 xfailed`).
- 2026-02-08: Continued Stage 5 legacy-flag cleanup for MCP by removing unused `MCP_RATE_LIMIT_USE_REDIS` from `app/core/MCP_unified/config.py` and aligning MCP rate-limit docs to RG backend controls (`RG_BACKEND=redis` + `REDIS_URL`) in `app/core/MCP_unified/README.md`, `Docs/Deployment/Operations/MCP_Rate_Limits_Tuning.md`, and `Docs/Published/Deployment/Operations/MCP_Rate_Limits_Tuning.md`. Revalidated with focused MCP/RG suites (`11 passed` across `test_rg_cutover_embeddings_mcp.py` and `app/core/MCP_unified/tests/test_regression_behaviors.py`).
- 2026-02-08: Revalidated the full Resource Governance regression suite after the MCP legacy-flag cleanup: `python -m pytest -q tldw_Server_API/tests/Resource_Governance` => `135 passed, 14 skipped, 2 xfailed` in this environment.
- 2026-02-08: Continued Stage 5 shim retirement by removing the remaining Evaluations RG-unavailable compatibility marker (`legacy_fallback_mode`) from `app/core/Evaluations/user_rate_limiter.py`; RG-unavailable path now returns only RG metadata (`policy_id`, `rate_limit_source`) and does not expose legacy shim fields. Updated cutover coverage in `tests/Resource_Governance/test_rg_cutover_evals_authnz_character_web.py::test_evaluations_rg_unavailable_uses_diagnostics_only_shim` to assert legacy shim metadata keys are absent.
- 2026-02-08: Continued Stage 5 docs/examples cleanup by aligning generated docs-site MCP rate-limit references with RG-only backend settings in `Docs/site/Deployment/Operations/MCP_Rate_Limits_Tuning/index.html` and `Docs/Docs/site/Deployment/Operations/MCP_Rate_Limits_Tuning/index.html` (`RG_BACKEND=redis` + `REDIS_URL`, no `MCP_RATE_LIMIT_USE_REDIS`/`MCP_REDIS_URL`).
- 2026-02-08: Revalidated after the above cleanup with focused suites (`19 passed` across `test_rg_cutover_evals_authnz_character_web.py`, `test_rg_cutover_embeddings_mcp.py`, and `app/core/MCP_unified/tests/test_regression_behaviors.py`) and full RG regression (`135 passed, 14 skipped, 2 xfailed`).
- 2026-02-08: Continued Stage 5 docs/examples cleanup by updating `Docs/MCP/Unified/System_Admin_Guide.md` rate-limit configuration to RG backend envs (`RG_BACKEND`, `REDIS_URL`) and removing obsolete `MCP_RATE_LIMIT_STORAGE` guidance. Verified no remaining retired limiter env references in active docs (excluding Product/Plans/static-site mirrors): no matches for `MCP_RATE_LIMIT_USE_REDIS`, `MCP_RATE_LIMIT_STORAGE`, `AUDIO_QUOTA_USE_REDIS`, `WORKFLOWS_QUOTA_DAILY_PER_USER`, `WORKFLOWS_QUOTA_BURST_PER_MIN`, or `WORKFLOWS_DISABLE_RATE_LIMITS`.
- 2026-02-08: Continued Stage 5 cleanup for operator clarity by updating residual Chat/Embeddings RG error logs to reflect diagnostics-only shim behavior (no “falling back to legacy limiter” wording) in `app/core/Chat/rate_limiter.py` and `app/core/Embeddings/rate_limiter.py`. Revalidated with focused RG cutover/bypass suites (`9 passed` across `test_chat_rg_limiter_cutover.py`, `test_rg_cutover_embeddings_mcp.py`, and `test_rg_single_source_bypass.py`).
- 2026-02-08: Continued Stage 5 cleanup for embeddings-server RG parity messaging by updating `app/core/Embeddings/Embeddings_Server/Embeddings_Create.py` logs to reflect the current fail-closed behavior when RG is enabled but unavailable (`legacy token-bucket fallback is retired`) instead of mentioning legacy fallback. Revalidated with focused embeddings-server cutover suites (`6 passed` across `test_rg_cutover_embeddings_server.py` + `test_rg_cutover_embeddings_mcp.py`) and full RG regression (`135 passed, 14 skipped, 2 xfailed`).
- 2026-02-08: Continued Stage 5 shim retirement for Prompt Studio rate limiting by removing the process-local fallback limiter path in `app/api/v1/API_Deps/prompt_studio_deps.py::check_rate_limit`; when the shared limiter path is unavailable, behavior is now diagnostics-only allow (one-shot warning) rather than local per-process counter enforcement. Added regression coverage in `tests/prompt_studio/unit/test_prompt_studio_deps_headers.py::test_prompt_studio_rate_limit_shared_limiter_failure_uses_diagnostics_only_shim` and revalidated with focused suites (`9 passed` across prompt-studio deps headers + `test_rg_single_source_bypass.py`) and full RG regression (`135 passed, 14 skipped, 2 xfailed`).
- 2026-02-08: Continued Stage 5 ingress shim retirement for Evaluations by converting `check_evaluation_rate_limit` in `app/api/v1/endpoints/evaluations/evaluations_auth.py` to diagnostics-only (no legacy per-minute 429 enforcement). The dependency now skips when RG policy metadata is present and otherwise emits a one-shot warning while allowing requests, preserving RG as the sole limiter source. Added RG regression coverage in `tests/Resource_Governance/test_rg_single_source_bypass.py::test_evaluations_check_rate_limit_is_diagnostics_only_shim` and revalidated with focused suites (`16 passed` across `test_rg_single_source_bypass.py`, `test_rg_cutover_evals_authnz_character_web.py`, and `test_evaluations_stage1_route_and_error_regressions.py`) plus full RG regression (`135 passed, 14 skipped, 2 xfailed`).
- 2026-02-08: Continued Stage 5 shim retirement for AuthNZ auth ingress by removing DB fallback-limiter enforcement from `app/api/v1/endpoints/auth.py::_reserve_auth_rg_requests` when RG is enabled but unavailable/policy-missing/reserve-failing; these paths now emit one-shot diagnostics and allow (RG remains sole enforcer). Removed the obsolete auth fallback policy-limit table and fallback limiter wiring, added one-shot diagnostics helper state, and kept explicit RG deny behavior unchanged. Updated/addded unit + RG regression coverage in `tests/AuthNZ/unit/test_auth_endpoints_extended.py` (governor-missing/policy-missing/reserve-failure diagnostics-only assertions) and `tests/Resource_Governance/test_rg_single_source_bypass.py::test_auth_endpoint_reserve_uses_diagnostics_only_shim_when_governor_unavailable`. Revalidated with focused suites (`4 passed` in AuthNZ reserve slice; `4 passed` in RG single-source bypass; `2 passed` in authnz-debug E2E header parity) and full RG regression (`137 passed, 14 skipped, 2 xfailed`).
- 2026-02-08: Continued Stage 5 shim retirement for AuthNZ dependency limiters by making `app/api/v1/API_Deps/auth_deps.py::{check_rate_limit,check_auth_rate_limit}` diagnostics-only whenever `RG_ENABLED=1` but request policy context is missing (`request.state.rg_policy_id` absent). In this RG-enabled/no-policy branch, legacy fallback limiter enforcement is now bypassed with one-shot diagnostics warnings to preserve RG single-source semantics; legacy fallback checks remain only for explicit `RG_ENABLED=0` deployments. Added coverage in `tests/AuthNZ_Unit/test_auth_deps_hardening.py` for both dependency paths and revalidated with focused slices (`3 passed` for new hardening tests; `4 passed` in `test_rg_single_source_bypass.py`) plus full RG regression (`137 passed, 14 skipped, 2 xfailed`).
- 2026-02-08: Continued Stage 5 AuthNZ dependency cleanup by retiring the remaining `RG_ENABLED=0` legacy enforcement path in `app/api/v1/API_Deps/auth_deps.py::check_auth_rate_limit`; the dependency is now diagnostics-only regardless of RG enabled state when ingress policy context is missing, while preserving claim-first principal handling (`is_single_user_principal`) and governor-hook invariants. Removed obsolete fallback-limiter branches (`check_rate_limit_fallback` / `check_rate_limit` enforcement + 429 path) from this dependency, expanded one-shot diagnostics reasons (`rg_enabled_without_policy_context`, `rg_disabled_legacy_limiter_retired`), and updated AuthNZ hardening coverage (`tests/AuthNZ_Unit/test_auth_deps_hardening.py`) to assert no legacy fallback limiter calls under both `RG_ENABLED=1` and `RG_ENABLED=0`. Completed docs/env sweep for AuthNZ deps/operator guidance in `Docs/Code_Documentation/Guides/AuthNZ_Code_Guide.md`, `Docs/Published/Code_Documentation/Guides/AuthNZ_Code_Guide.md`, `Docs/Design/AuthNZ_RG_Coverage_Stage1_2026_02_07.md`, `Docs/User_Guides/Production_Hardening_Checklist.md`, and `Docs/Published/User_Guides/Production_Hardening_Checklist.md`. Revalidated with focused hardening suite (`17 passed`) and full RG regression (`139 passed, 14 skipped, 2 xfailed`).
- 2026-02-09: Continued Stage 5 AuthNZ dependency cleanup by retiring the remaining `RG_ENABLED=0` legacy enforcement path in `app/api/v1/API_Deps/auth_deps.py::check_rate_limit`; both AuthNZ dependency shims (`check_rate_limit`, `check_auth_rate_limit`) are now diagnostics-only when ingress policy context is missing, regardless of RG enabled state. Updated hardening coverage (`tests/AuthNZ_Unit/test_auth_deps_hardening.py`) to replace the old IP fallback assertion with diagnostics-only expectations under `RG_ENABLED=0`, and aligned operator/docs language for AuthNZ dependency behavior in `Docs/Code_Documentation/Guides/AuthNZ_Code_Guide.md`, `Docs/Published/Code_Documentation/Guides/AuthNZ_Code_Guide.md`, `Docs/Operations/Env_Vars.md`, and `Docs/Published/Env_Vars.md`. Revalidated with focused hardening (`17 passed`) and full RG regression (`139 passed, 14 skipped, 2 xfailed`).
- 2026-02-08: PRD review and documentation pass. Updated `Docs/Product/Completed/AuthNZ-Refactor/Resource_Governor_PRD.md` to close open questions, add shadow-mode exit criteria (mismatch rate <1% for 7+ days), backward compatibility section, additional implementation scope not in original PRD, expanded risks, and updated deletions list. Extracted AuthNZ lockout tracking to dedicated `app/core/AuthNZ/lockout_tracker.py` module (`LockoutTracker` class + `get_lockout_tracker()` singleton); `rate_limiter.py` now delegates lockout methods. Added 6 tests in `tests/AuthNZ/unit/test_lockout_tracker.py` and updated `test_rate_limiter_lockout_reset.py` for new delegation. Documented RG integration status in `Evaluations/user_rate_limiter.py` docstring (cost caps legacy-only). Added 2 cutover tests for evaluations cost-cap enforcement when RG allows. Added deprecation notices to `Chat/rate_limiter.py`, `Embeddings/rate_limiter.py`, `Embeddings_Create.py::TokenBucketLimiter`, and `Web_Scraping/enhanced_web_scraping.py::RateLimiter`. Created comprehensive Resource Governor section in `Docs/Operations/Env_Vars.md` and `Docs/Published/Env_Vars.md` documenting all `RG_*` env vars and marking legacy rate limit knobs as deprecated. Revalidated with 30 passing tests (23 cutover + 7 AuthNZ lockout).
