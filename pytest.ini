[pytest]
markers =
    unit: unit tests (fast)
    integration: integration tests
    pg_integration: requires a live Postgres + pgvector (skips if unavailable)
    performance: performance and scale tests
    requires_pypff: tests that require pypff/libpff to be installed
